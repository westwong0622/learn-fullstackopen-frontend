import React, { useState } from 'react';
import ReactDOM from 'react-dom';

const App = (props) => {
  return (
    <div>
      Hello
    </div>
  )
}

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
